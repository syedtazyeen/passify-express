import { NextFunction } from "express";

export const signInService = (req: Request, res: Response, next: NextFunction) => {
    try {

    } catch (error) {
        throw error
    }
}


export const signUpService = (req: Request, res: Response, next: NextFunction) => {
    try {

    } catch (error) {
        throw error
    }
}


